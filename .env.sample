# Copy to .env and fill in. By default .env is bundled into the Lambda package
# (see serverless.yml package.include). For production secrets, prefer Lambda
# environment variables (provider.environment) or SSM/Secrets Manager.

APP_NAME=
S3_BUCKET_FOR_BERROCK=